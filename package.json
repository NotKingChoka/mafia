{
  "name": "mafia-online",
  "version": "1.0.0",
  "private": true,
  "description": "Online Mafia game with rooms, roles, phases, chat and deploy-ready frontend/backend.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c red,blue \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w frontend",
    "start": "npm run start -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
